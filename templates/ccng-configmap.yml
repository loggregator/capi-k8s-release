#@ load("@ytt:data","data")
#@ load("@ytt:yaml","yaml")
#@ load("ccng-config.lib.yml","ccng_config")
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloud-controller-ng
  namespace: cf-system
data:
  cloud_controller_ng.yaml: #@ yaml.encode(ccng_config())
